# Copy this file to .env and fill in local values.
# Add one AI provider before job evaluation/scoring.
# The three model names must belong to this same provider/API key.

AI_PROVIDER_NAME=
AI_API_KEY=
AI_BASE_URL=

PRIMARY_MODEL=
FALLBACK_MODEL=
SECOND_FALLBACK_MODEL=
